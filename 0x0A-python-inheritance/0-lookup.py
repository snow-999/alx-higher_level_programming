#!/usr/bin/python3
"""function to return the list"""


def lookup(obj):
    """look up function
    arg:
        obj: the object that will get the docmnebts inside
    return: the doucments"""
    return dir(obj)
